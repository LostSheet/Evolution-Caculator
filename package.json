{
  "name": "ark-passive-simulator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "extract:core": "node scripts/extract-core.mjs",
    "test": "npm run test:pareto && npm run test:runner && npm run test:legacy",
    "test:pareto": "node scripts/pareto.test.mjs",
    "test:runner": "node scripts/runner.test.mjs",
    "test:legacy": "node scripts/diff-legacy.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.4",
    "vite": "^8.1.1"
  }
}
