# 명세 — 답이 먼저 서는 개편

2026-08-20 · 설계: Fable 세션 · 대상 독자: 구현 세션

한 줄 요약: **설정은 "확인하는 층 + 파고드는 층"으로 접고, 결과는 대표 세팅
넉 장이 먼저 서고, 그 카드가 편집의 출발점이 된다.**

---

## 0. 구현 전에 읽을 것 — 이 리포의 함정

1. **CRLF.** `legacy/app.js` · `src/app.css` · `src/App.svelte` ·
   `src/lib/store.svelte.js` · `src/lib/components/PageSetup.svelte`는 CRLF다.
   node 스크립트의 여러 줄 `.replace()`는 **조용히 실패한다**. 반드시 Edit
   도구로 고칠 것.
2. **생성 파이프라인.** 계산 모델의 원본은 `legacy/*.js`다. 고칠 때는
   legacy 먼저 → `scripts/extract-core.mjs`의 `names:` 배열에 새 선언을
   추가 → `npm run extract:core`. names에 안 올리면 런타임 ReferenceError.
   `src/lib/core/runner.js`는 손으로 쓰는 파일이라 직접 고친다.
3. **CSS.** 같은 선택자를 아래에 덧붙이지 말 것 — 옛 블록의 속성이 조용히
   살아남는다. 기존 블록을 고친다. `:has()`를 다른 선택자와 한 목록에 묶지
   말 것 — 미지원 브라우저에서 목록 전체가 죽는다.
4. **UI 문구.** 조작 요소·표식에는 **값과 라벨만. 기전을 설명하는 문장 금지.**
   (예: `점 클릭 = 고르기` ○ / "점을 누르면 그 세팅이 적용됩니다" ×)
   빈 상태 안내문만 짧은 문장 허용 — 기존 관례.
5. **스코프.** 이 문서에 없는 개선을 얹지 말 것. 발견한 문제는 구현하지 말고
   보고만 한다.
6. **node 경로.** `PATH="/c/Program Files/nodejs:$PATH"`를 앞에 붙여야
   npm/node가 잡힌다.
7. **커밋.** `docs/` 빌드 산출물 포함해 커밋·푸시. `armory-*` 덤프는 절대
   푸시 금지(캐릭터명 포함). `.env.local`은 열지도 읽지도 않는다.
8. Svelte 5 룬 사용. `{@const}`는 블록 직계 자식 자리에만 온다.

## 검증 명령

```
PATH="/c/Program Files/nodejs:$PATH" npm test        # 전부 통과해야 함
PATH="/c/Program Files/nodejs:$PATH" npm run build   # docs/ 생성
```

`diff-legacy` 4000판 0 실패가 기준선이다. 브라우저 검증은 각 작업의 수용
기준을 dev 서버(포트 5173, launch.json `dev`)에서 직접 확인한다.

---

## 1. 정해진 것 (재논의 없이 이대로)

| 결정 | 내용 |
|---|---|
| 대표 카드 4종 | 한 방 딜 최고 · DPS 최고 · 쿨감 한계 최강 · 대난투 최강 |
| 대난투 기준 | 무력화 가중 **100% 고정**의 한 방 딜 지수. 세부 설정의 '대난투 딜 비중 %'와 무관 |
| 곡선 | 대난투는 곡선·축 후보에 넣지 않는다 |
| 설정 위층 | 각인 후보 · 고정 노드 · 1T·특화 · 자버프 요약(읽기 전용 + 바로가기) |
| 설정 아래층 | 파티 시너지 · 하한·상한 · 대난투 비중 · 펫·음식 (접힘 기본) |
| 자버프·딜 비중·특화 묶음 | 편집 자리는 깨달음 페이지 그대로. 설정 쪽에는 상태와 바로가기만 |
| '내 빌드로' 후 | 결과 화면에 머무른다. 페이지 이동 없음 (여러 장 견주는 흐름을 안 끊는다) |
| 실행 단추 | 상단바 하나만. 나머지 실행·이동 중복은 제거 |

---

## 2. 작업

### 작업 1 — 엔진: 대난투 지수와 네 번째 순위 목록

**무엇.** 탐색이 후보마다 `staggerIndex`(무력화 가중 100%일 때의 한 방 딜
지수)를 계산하고, 결과에 `results.stagger` 순위 목록(기존 damage/dps/cooldown
목록과 같은 크기)을 추가한다.

**어디.** `legacy/app.js`의 finalizeMetrics(또는 그 근처)가 staggerIndex를
내놓게 하고 extract-core names에 추가. 탐색 평가·결과 조립은
`src/lib/core/runner.js`와 그 주변.

**동작.**
- staggerIndex = 무력화 대상 피해 그룹(`STAGGER_DAMAGE_GROUPS`)의 가중을
  1.0으로 둔 한 방 딜 지수. 사용자의 '대난투 딜 비중 %' 설정값과 무관하다.
- 하한·상한 필터는 다른 목록과 동일하게 적용.
- **빔 함정(핵심).** 제압·부러진 뼈 위주 조합은 일반 지수에서 밀려 빔이 일찍
  잘라낸다. 빔의 보존 기준을 "지표별 상위 보존의 합집합"으로 넓혀
  stagger 상위가 최종 평가까지 살아남게 할 것. 이걸 빼먹으면 stagger 목록이
  차선만 모은 쓰레기가 된다.

**수용 기준.**
- 제압을 후보에 넣고 돌리면 `results.stagger` 1위에 제압 계열 노드가 든다.
- 작은 공간 전수 탐색과 빔 탐색의 stagger 1위가 일치한다 —
  `scripts/floors.test.mjs`에 (k) 케이스로 추가. (주의: 이 파일은 요약
  출력이 파일 끝에 있어야 한다. 중간에 process.exit를 두지 말 것.)
- diff-legacy 4000판 0 실패 유지.

### 작업 2 — 결과 페이지: 대표 카드 넉 장

**무엇.** `PageResults.svelte`에서 곡선 카드 위에 대표 카드 줄을 세운다.
새 컴포넌트 `src/lib/components/TopPicks.svelte` 권장.

**동작.**
- 카드 4장: `한 방 딜 최고` = results.damage[0] · `DPS 최고` = results.dps[0]
  · `쿨감 한계 최강` · `대난투 최강` = results.stagger[0].
- **쿨감 한계 최강**: 카드에 `내 한계 %` 숫자 입력 하나. 값이 있으면 그 %가
  든 한계 구간의 1위(sweepCeiling/ceilingLabel 재사용), 비어 있으면 지금의
  챔피언(가장 넓은 구간). 입력값은 새로고침 후에도 유지(persist).
- 카드 내용: 라벨, 한 방 딜·DPS(각각 내 빌드 대비 증감 — 하단 막대와 같은
  ±% 규칙), 쿨감 %, 치적(결과 표와 동일한 `80% (116.81%)` 상한 표기),
  1T 요약, 단추 [담기] [내 빌드로].
- 카드 몸통 클릭 = 고르기(app.selectedId) — 줄·점 클릭과 같은 동작.
  임시 열·곡선 십자선이 따라온다.
- 같은 조합이 여러 기준의 1위면 카드 하나로 합치고 라벨을 나란히 단다.
- 대난투 1위의 staggerIndex 표기는 그 카드 안에서만 (다른 카드는 기존 지표).
- results가 없으면 카드 줄 없음. 빈 상태 카드 문구를
  "돌리면 대표 세팅 네 장과 균형 곡선이 여기 섭니다."로 갱신.

**수용 기준.** 탐색 후 카드 4장(또는 합쳐진 3장 이하)이 곡선 위에 서고,
한계 % 입력을 40으로 바꾸면 카드가 그 구간의 1위로 바뀐다. 카드의 증감
부호·수치가 하단 막대의 임시 열과 일치한다.

### 작업 3 — store: '내 빌드로' (adoptResult)

**무엇.** `store.svelte.js`에 `adoptResult(entry)` 하나.

**동작.**
1. 비교함이 가득(5)이면 거절하고 `app.status`에 기존 가득참 문구.
2. 지금 내 빌드를 얼려 담는다. 이때 이름이 정확히 `내 빌드`(기본값)면
   `freeName("이전 빌드")`로 자동 개명 — **같은 규칙을 makeMine의 얼림에도
   적용**한다(기존 이름 충돌 버그의 수리).
3. `stateFromResult(entry)`를 내 빌드로 올리고 `app.buildName =
   nextSearchName()` 계열로 명명(담기와 같은 규칙).
4. `app.status`에 "'…'을 내 빌드로 …" 안내(기존 makeMine 문구 재사용 수준).
5. 페이지 이동 없음.

**쓰는 곳.** 대표 카드(작업 2)와 자세히 대화상자(작업 8)의 [내 빌드로].

**수용 기준.** 카드에서 내 빌드로 → 하단 막대 첫 칸이 그 세팅으로 바뀌고,
이전 빌드가 얼린 열로 남는다. 기본 이름 상태에서 해도 '내 빌드'라는 얼린
열이 생기지 않는다.

### 작업 4 — 설정 페이지 2층화

**무엇.** `SearchScope.svelte`의 카드들을 접힘 카드(details/summary,
`.card-fold` — PageSetup의 기존 패턴·CSS 재사용)로 바꾸고 두 층으로 배치.

**동작.**
- 위층(이 순서대로): **각인 후보**(요약: `고정 2 · 후보 1 · 제외 19`) ·
  **고정 노드**(요약: `고정 0개` 또는 `치명 30 고정 외 1`) ·
  **1T·특화**(기존 '탐색 범위' 카드 개명 — 1T 배분 단위 + 특화 30 고정,
  요약: `1Lv 단위 · 특화 30 고정`) · **자버프**(읽기 전용 요약:
  `5줄 · 유효율 평균 87%` 또는 `없음`, 펼치면 딜 비중·특화 묶음 상태 한 줄씩
  + [깨달음으로 →] 단추 = goTab("awakening")).
- 아래층: 파티 시너지 · 하한·상한 · 대난투 비중 · 펫·음식.
- 접힘 기본. 펼침 상태는 localStorage에 저장(PageSetup의 isOpen/setFold
  관례 재사용).
- '이번 탐색' 요약 카드(조합 수)는 오른쪽 레일에 유지.
- 편집 기능 자체는 어느 카드도 잃지 않는다 — 접히고 요약이 생길 뿐.

**수용 기준.** 접힌 채로 위층 넉 줄만 훑어도 무엇이 걸려 있는지 읽힌다.
자버프 카드에서 깨달음으로 건너가고, 거기서 고친 유효율이 돌아왔을 때
요약에 반영된다.

### 작업 5 — 실행 단추 통일

- `PageResults.svelte` runbar: `← 탐색 설정`·`다시 탐색` 단추 제거.
  진행 막대·중지·상태 문구만 남긴다.
- `SearchScope.svelte` 맨 아래 `이 범위로 탐색 →` 제거.
- 상단바의 `탐색 실행 / 다시 탐색`(빌드 축에서는 `탐색으로 →`)이 유일한
  실행 자리다. 빈 결과 카드의 `탐색 설정으로 →`는 유지(길 안내).

### 작업 6 — 상태줄 전역화

**무엇.** `app.status`가 결과 페이지 runbar에서만 보이던 것을, 하단 막대
(StatusBar)에서 어느 화면에서든 잠깐 보이게.

**동작.** app.status가 바뀌면 하단 막대 위에 작은 말풍선/줄로 4초쯤 표시 후
사라짐. 결과 runbar의 표시는 유지(탐색 요약이 산다). 세팅 저장·비교함
가득참·맞바꾸기 문구가 이제 실제로 보인다.

**수용 기준.** 설정 페이지에서 상단 '세팅' 저장 → 하단에 "세팅을
저장했습니다."가 떴다 사라진다. 비교함 5개 상태에서 6번째 담기 체크 →
거절 이유가 보인다.

### 작업 7 — 결과 낡음 띠

**무엇.** 탐색 완료 시점의 캐릭터 서명을 결과에 저장하고, 이후 캐릭터가
바뀌면 결과 화면 상단(카드 줄 위)에 표식 띠를 세운다.

**동작.**
- 서명 = 탐색이 덮어쓰는 차원(nodeLevels · engravings · engravingStones ·
  펫 · 음식)을 뺀 캐릭터 상태의 직렬화.
- 다르면 띠: `세팅 바뀜 · 숫자는 돌린 시점 기준` — 라벨만, 단추 없음
  (다시 탐색은 상단바가 늘 들고 있다).
- 다시 탐색하면 띠가 사라진다.

**수용 기준.** 탐색 → 사전 세팅에서 무기 품질 변경 → 결과로 오면 띠가 있다.
다시 탐색하면 없다. 노드 레벨만 바꾼 경우(탐색이 덮는 차원)에는 띠가 없다.

### 작업 8 — 자세히 대화상자 정비

- 치적을 결과 표와 같은 상한 표기로(`80.00% (116.81%)` — bluntThorn이고
  합산이 상한 초과일 때만 괄호).
- 대표값(한 방 딜·DPS) 옆에 내 빌드 대비 증감 2개.
- [비교함에 담기] 옆에 [내 빌드로] 추가(작업 3 재사용).

### 작업 9 — 상세 단추 상시 노출

`.gauge-call { display: none }`과 1240px 미디어 쿼리 해제 — 폭과 무관하게
노출(사전 세팅 탭 제외는 유지). 넓은 화면 진화 노드 탭에서 레일과 중복되는
것은 허용(같은 내용의 다른 문).

---

## 3. 구현 순서와 검증

권장 순서: **1(엔진) → 3(store) → 2(카드) → 4(설정) → 5 → 6 → 7 → 8 → 9.**
작업 1과 3이 끝날 때마다 `npm test` 전체를 돌리고, UI 작업은 각 수용 기준을
브라우저에서 확인한 뒤 다음으로 간다. 마지막에 build → docs 포함 커밋 —
커밋 제목은 지금 관례대로 한국어 한 문장.

## 4. 하지 말 것

- 결과 표·곡선·비교함·서랍의 기존 동작 변경(이 문서에 적힌 것 외).
- 대난투용 DPS 변형 지수, 대난투 축의 곡선 — 만들지 않는다.
- 설정 항목의 이동(자버프 편집을 설정 페이지로 옮기는 것 등) — 바로가기만.
- 새 저장 체계 — 폴드 상태 외의 새 localStorage 키 남발 금지.
- 이 문서 밖의 리팩터링.
