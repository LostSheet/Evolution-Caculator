# 명세 v2 — 비교 체계 개편, 그리고 답이 먼저

2026-08-25 · 설계: Fable 세션 · 구현: Opus 세션
이 문서가 유일한 사양원이다. v1(답이 먼저 서는 개편)을 대체한다 —
v1의 작업 중 살아남는 것은 2부에 다시 적혀 있고, 여기 없는 v1 내용은 죽은 것이다.

한 줄 요약: **비교의 닻을 하나로 세우고(같은 전제 위에서), 고른 하나를
크게 읽는 루프를 되살리되, 모아둔 여럿을 잃지 않는다. 그 위에 답 카드를 얹는다.**

---

## 0. 구현 전에 읽을 것 — 이 리포의 함정

1. **CRLF.** `legacy/app.js` · `src/app.css` · `src/App.svelte` ·
   `src/lib/store.svelte.js` · `src/lib/components/PageSetup.svelte`는 CRLF다.
   node 스크립트의 여러 줄 `.replace()`는 **조용히 실패한다**. 반드시 Edit
   도구로 고칠 것. 다른 파일도 고치기 전에 개행을 확인한다.
2. **생성 파이프라인.** 계산 모델의 원본은 `legacy/*.js`다. 고칠 때는
   legacy 먼저 → `scripts/extract-core.mjs`의 `names:` 배열에 새 선언 추가 →
   `npm run extract:core`. names에 안 올리면 런타임 ReferenceError.
   `src/lib/core/runner.js`는 손으로 쓰는 파일이라 직접 고친다.
3. **CSS.** 같은 선택자를 아래에 덧붙이지 말 것 — 옛 블록이 조용히 살아남는다.
   기존 블록을 고친다. `:has()`를 다른 선택자와 한 목록에 묶지 말 것.
4. **UI 문구.** 조작 요소·표식에는 **값과 라벨만. 기전을 설명하는 문장 금지.**
   빈 상태 안내문만 짧은 문장 허용.
5. **스코프.** 이 문서에 없는 개선을 얹지 말 것. 발견한 문제는 보고만 한다.
6. **node 경로.** `PATH="/c/Program Files/nodejs:$PATH"`를 앞에 붙인다.
7. **커밋.** `docs/` 빌드 포함 커밋·푸시. `armory-*` 덤프 푸시 금지.
   `.env.local`은 열지도 읽지도 않는다.
8. Svelte 5 룬. `{@const}`는 블록 직계 자식 자리에만.

검증 명령:

```
PATH="/c/Program Files/nodejs:$PATH" npm test        # 전부 통과 유지
PATH="/c/Program Files/nodejs:$PATH" npm run build   # docs/ 생성
```

diff-legacy 4000판 0 실패가 기준선. UI 작업은 각 수용 기준을 dev 서버
(launch.json `dev`)에서 브라우저로 확인하고 다음 작업으로 간다.

---

## 1. 개념 결정 (불변 — 재논의 없음)

1. **빌드 = 캐릭터 전부.** 진화 노드·각인·펫·사전 세팅·깨달음까지 통째.
2. **비교함에는 빌드가 들어간다.** 탐색 결과도 담는 순간 빌드(스냅샷)다.
3. **탐색 = 고정부 + 조건부.** 고정부(사전 세팅·깨달음·자버프·딜 비중·시너지)는
   내 빌드에서 깔리고, 조건부(노드·각인·펫·음식)는 조건 아래 탐색이 굴린다.
4. **비교는 같은 전제끼리만.** 전제가 다른 두 숫자에 증감을 달지 않는다.

### 용어 (코드·화면에서 일관되게)

| 용어 | 뜻 |
|---|---|
| 닻 | 모든 증감의 기준. 내 빌드(전제 초과 시 `내 배분 · 전제 적용`) |
| 초점 | 지금 견주는 하나. 표 줄·곡선 점·타일 어디서 골라도 이 하나로 온다 |
| 타일 | 비교함에 담긴 빌드가 하단 막대에 작게 서는 것 |
| 전제 | 탐색이 후보에게 입히는 각인 단계 (app.search.engravingTiers) |
| 실물 | 불러온 캐릭터가 실제로 낀 각인 단계 (app.character.engravings) |

## 2. 전제 규칙 (결정 3줄)

1. 증감은 같은 전제끼리만 — 낀 각인 중 하나라도 전제 단계 > 실물 단계면
   "전제 초과"이고, 실물 세팅은 증감 자격을 잃는다.
2. 그때 닻 자리는 `내 배분 · 전제 적용`이 승계한다 — 내 노드·각인 종류는
   그대로, 각인 단계만 전제로 치환해 계산한 것. 실물은 닻 밑에 꼬리표
   한 줄로 남는다: `실물 −8.2%`.
3. 전제 기본값 = 보유 단계. 캐릭터를 불러올 때 장착 5종의 실제 단계를
   전제에 자동으로 채우고(실물이 사실이므로 덮어씀), 비장착 각인의 단계는
   캐릭터별 저장(기존 ENGRAVING_SCOPE 저장소)이 기억한다. 전제 초과는
   유저가 단계를 손으로 올렸을 때만 생긴다.

전제 치환은 **닻에만** 적용한다. 타일(담긴 빌드)은 저장된 그대로 계산한다.

---

## 3. 1부 — 비교 체계 (우선 구현)

### 작업 1 — 전제 엔진 (store)

**무엇.** 닻 상태를 만드는 함수와 초과 판정.

- `premiseExceeded()`: 낀 각인별로 전제 단계와 실물 단계를 비교 (단계 비교는
  기존 각인 사다리 인덱스 사용). 하나라도 전제 > 실물이면 true.
- `anchorState()`: 초과가 아니면 `app.character` 그대로. 초과면 사본에서
  낀 각인들의 단계만 전제로 치환한 상태.
- 캐릭터 불러오기(applyCharacter)에서 장착 각인의 전제 단계를 실물로 채움.
  비장착 각인 단계는 건드리지 않는다.
- 닻 지표 = calculateMetrics(anchorState()). 실물 꼬리표 값 =
  (실물 지표 / 닻 지표 − 1)로 두 지표 각각.

**수용 기준.** 전제를 실물보다 올리면 하단 막대 닻이 `전제 적용` 배지와
실물 꼬리표를 달고, 내리면 원래대로 돌아온다. 초과가 아닐 때 닻 숫자는
지금과 동일하다.

### 작업 2 — 하단 막대 재편: 닻 + 초점 + 타일

**무엇.** `StatusBar.svelte`(및 App.svelte의 슬롯 조립)를 세 구획으로 재편.

```
[ 내 세팅 4,528 · 6,544 ]  [ 초점: 탐색 #14  한 방 −2.7% · DPS +9.1% ]  |타일들| ^ 비교함
```

- **닻(왼쪽 고정)**: 빌드 이름 + 두 수치. 숫자에 `한 방` `DPS` 꼬마 라벨.
  전제 초과 시 이름이 `내 배분 · 전제 적용`, 밑에 `실물 −8.2%` 꼬리표.
- **초점(크게, 하나뿐)**: 이름 + 한 방 ±% · DPS ±% (닻 대비, 지표별 따로).
  임시 후보(아직 안 담김)면 `담기` 단추가 붙는다 — 누르면 타일로 굳는다.
  초점이 없으면 구획 자체를 접는다.
- **타일(작게, 여럿)**: 담긴 빌드마다 이름 + ±% 한 쌍. 클릭 = 초점으로
  (맞바꾸기 아님 — 파괴 동작은 막대에서 전부 제거). 초점인 타일은 강조.
  넘치면 가로 스크롤.
- **초점의 출처**: 결과 표 줄 클릭, 곡선 점 클릭, 타일 클릭, (2부) 대표 카드.
  store에 판별 가능한 초점 상태를 둔다 — 예: `app.focus = { kind: "result"|"tile", id }`.
  기존 selectedId·임시 열(previewed) 메커니즘은 이 구조로 흡수한다.
- **↑/↓ 키**: 결과 탭에서 표의 줄 이동(초점이 따라온다). 입력 칸에
  포커스가 있을 때는 개입하지 않는다.
- 기존 "막대 열 클릭 = 맞바꾸기"는 삭제. makeMine 호출 경로는 작업 3의
  단추들만 남긴다.

**수용 기준.** 표에서 ↑/↓로 줄을 옮기면 초점 ±%가 즉시 갱신된다. 타일을
누르면 그 빌드가 초점으로 서고 내 빌드는 바뀌지 않는다. 임시 초점에서
`담기`를 누르면 타일이 하나 늘고 초점은 그 타일이 된다.

### 작업 3 — 드로어: 내 빌드로 + 라벨

- 드로어 비교표의 얼린 열 머리마다 `내 빌드로` 단추. 동작 = 기존 맞바꾸기
  (그 빌드가 내 빌드로, 쓰던 것은 그 자리에 얼림). 자세히 대화상자에도
  같은 단추(탐색 후보용 — stateFromResult 경유).
- 자동 개명 규칙: 얼리는 쪽 이름이 정확히 `내 빌드`(기본값)면
  `freeName("이전 빌드")`로 바꿔 얼린다. makeMine·담기 모두 적용.
- 드로어·막대의 두 수치에 `한 방` `DPS` 라벨. 굵기 차이만으로 구분하는
  지금 방식은 폐기.

**수용 기준.** 드로어에서 `내 빌드로` → 닻이 그 빌드로 바뀌고 이전 것이
같은 자리에 얼린다. 기본 이름 상태에서 해도 `내 빌드`라는 얼린 열이
생기지 않는다.

### 작업 4 — 탐색 표: 0번 행 + 증감 모드 + 차이 표기

- **0번 행**: 표 맨 위(머리글 바로 아래, 같이 붙박이)에 닻 행. 지표는 닻
  기준, 구성 칸은 내 배분. 시각적으로 후보 줄과 구분(배경 톤).
- **증감 모드**: 표 머리에 토글 `절대값 | 내 세팅 대비` (기본: 내 세팅 대비).
  증감 모드에서 한 방·DPS 칸이 닻 대비 ±%가 되고, 손실 열(1등 대비)은
  숨긴다 — 닻이 둘이면 헷갈린다. 절대값 모드는 지금 표 그대로.
- **차이 표기**: 초점이 있으면 각 줄의 1T·주요 노드 칸이 초점과의 차이만
  적는다 — `+아드레날린 −타격의 대가`, `혼강1→무마2`, `치명 27→23`.
  초점 줄 자신과 0번 행은 전체 구성 표시. 표 머리에 `전체 구성` 토글로
  끌 수 있다(기본: 차이만).
- 선택 상태 시각화는 초점과 일치시킨다(초점 줄 강조).

**수용 기준.** 표를 열면 0번 행이 닻이고 모든 줄이 ±%로 읽힌다. 줄 하나를
고르면 나머지 줄들이 그 줄과 다른 부분만 보여준다. 절대값 모드로 돌리면
지금 표와 같다.

### 작업 5 — 기준 카드 + 낡음 띠

- **설정 페이지 머리에 기준 카드 한 줄**: `기준 · {빌드 이름}` + 고정부
  요약(무기 품질 · 보석 쿨감 · 치피 · 딜 비중 · 시너지 줄 수 · 자버프 줄 수).
  탐색이 덮는 것(노드·각인·펫·음식)은 적지 않는다 — 그 부재가 곧
  "이건 후보가 덮는다"는 표시다.
- **결과 쪽 이름표**: 탐색 완료 시 고정부의 서명(고정부 직렬화)을 결과에
  저장. 결과 화면에 `'{이름}' 기준` 이름표. 지금 고정부와 다르면 이름표가
  띠가 된다: `기준 바뀜 · 돌린 시점 기준` — 라벨만, 단추 없음(다시 탐색은
  상단바가 늘 들고 있다).

**수용 기준.** 탐색 → 사전 세팅에서 무기 품질 변경 → 결과에 띠. 다시
탐색하면 사라짐. 노드 레벨만 바꾼 경우(조건부)에는 띠 없음.

### 작업 6 — 상태줄 전역화

`app.status`가 바뀌면 하단 막대 위에 잠깐(4초쯤) 떠서 사라지는 줄 하나.
결과 runbar의 표시는 유지. 세팅 저장·비교함 가득참·내 빌드로 안내가
어느 화면에서든 보이게 된다.

**수용 기준.** 설정 페이지에서 세팅 저장 → 하단에 문구가 떴다 사라진다.
비교함 5개 상태에서 여섯 번째 담기 → 거절 이유가 보인다.

---

## 4. 2부 — 답이 먼저 (1부 완료 후)

### 작업 7 — 엔진: 대난투 지수와 네 번째 순위 목록

- 후보마다 `staggerIndex`(무력화 대상 피해 그룹 가중 1.0의 한 방 딜 지수,
  '대난투 딜 비중 %' 설정과 무관) 계산, `results.stagger` 순위 목록 추가.
  legacy 먼저 고치고 extract. 하한·상한 동일 적용.
- **빔 함정(핵심).** 제압·부러진 뼈 조합은 일반 지수에서 밀려 빔이 일찍
  잘라낸다. 빔 보존 기준을 "지표별 상위 보존의 합집합"으로 넓혀 stagger
  상위가 최종 평가까지 살아남게 할 것.

**수용 기준.** 제압을 후보에 넣고 돌리면 stagger 1위에 제압 계열이 든다.
작은 공간 전수와 빔의 stagger 1위가 일치 — `scripts/floors.test.mjs`에
(k) 케이스 추가(요약 출력은 파일 끝 유지, 중간 process.exit 금지).
diff-legacy 0 실패 유지.

### 작업 8 — 결과 페이지: 대표 카드 넉 장

곡선 위에 카드 줄(`TopPicks.svelte` 권장): `한 방 딜 최고` = damage[0] ·
`DPS 최고` = dps[0] · `쿨감 한계 최강`(카드에 `내 한계 %` 입력 — 값이 있으면
그 %가 든 한계 구간의 1위, 비면 챔피언; persist) · `대난투 최강` = stagger[0].

- 카드: 라벨 · 두 지표(닻 대비 ±%) · 쿨감 · 치적(표와 같은 상한 표기) ·
  1T 요약 · 단추 [담기] [내 빌드로].
- **카드 몸통 클릭 = 초점** (작업 2의 초점 체계에 합류).
- 같은 조합이 여러 기준 1위면 카드 하나에 라벨을 나란히.
- 대난투 카드만 staggerIndex 표기.
- results 없으면 카드 없음. 빈 상태 문구:
  "돌리면 대표 세팅 네 장과 균형 곡선이 여기 섭니다."

**수용 기준.** 카드 ±%가 막대 초점의 ±%와 일치. 한계 % 입력 40 → 그 구간
1위로 교체. [내 빌드로] → 닻 교체 + 이전 것 얼림.

### 작업 9 — 설정 2층화 + 파티 시너지 간략

- 카드들을 접힘 카드(`.card-fold` 관례)로, 두 층 배치. 위층(순서대로):
  **기준 카드(작업 5)** · **각인 후보**(요약: `고정 2 · 후보 1 · 제외 19` —
  단계 칸의 의미는 "보유 단계", 전제 규칙과 연동) · **고정 노드**(요약:
  `고정 0개`) · **1T·특화**(기존 탐색 범위 개명) · **파티 시너지 간략**(아래) ·
  **자버프 요약**(읽기 전용 + [깨달음으로 →]).
  아래층: 파티 시너지 상세 · 하한·상한 · 대난투 비중 · 펫·음식.
- **파티 시너지 간략 = 일반 줄.** `legacy/synergy.js`에 직업 없는 줄 허용:
  job이 의사 코드 `"damage"` | `"critRate"`, 값은 SYNERGY_TYPES 표준
  (피해 증가 6% · 치적 10%), 가동율 기본 100, combatCount에 셈.
  getSynergyJob은 숫자만 아는 함수라 일반 줄은 그보다 앞에서 갈라 처리.
  normalizeSynergyRows가 의사 코드를 버리지 않게. job-buffs.json 불변.
  위층 카드: 스테퍼 둘 `피증 N`(0~3) · `치적 N`(0~3) — 일반 줄만 세고
  직업 줄은 건드리지 않음. 내 캐릭터 몫 한 줄(읽기 전용). 직업 줄 있으면
  `상세 N줄` + 바로가기. 상세 패널은 일반 줄을 `피해 증가 · 표준` 이름으로
  그리고 가동율 수정·지우기 가능.
- 접힘 상태는 localStorage(PageSetup의 isOpen/setFold 관례).

**수용 기준.** 피증 2 · 치적 1 → 계기판에 피해 증가 +12% · 치적 +10%.
일반 줄 하나 가동율 50 → +9%. 일반 줄과 직업 줄 혼재 시 각 줄 정확히
한 번 계산.

### 작업 10 — 잔손질

- **실행 단추 통일**: 결과 runbar의 `← 탐색 설정`·`다시 탐색` 제거(진행·
  중지·상태만), 설정 맨 아래 `이 범위로 탐색 →` 제거. 상단바가 유일한
  실행 자리. 빈 결과 카드의 `탐색 설정으로 →`는 유지.
- **자세히 대화상자**: 치적 상한 표기(표와 동일), 두 지표에 닻 대비 ±%.
  ([내 빌드로]는 작업 3에서 이미 붙음.)
- **상세 상시 노출**: `.gauge-call` display:none과 1240px 미디어 쿼리 해제 —
  폭 무관 노출(사전 세팅 탭 제외 유지). 넓은 노드 탭의 레일 중복 허용.

---

## 5. 구현 순서

**1부: 1 → 2 → 3 → 4 → 5 → 6, 2부: 7 → 8 → 9 → 10.**
작업 1·7 뒤에는 `npm test` 전체. UI 작업마다 수용 기준을 브라우저로 확인.
1부가 끝나면 커밋 한 번, 2부 끝나고 커밋 한 번 — 제목은 한국어 한 문장.
마지막에 build → docs 포함 푸시.

## 6. 하지 말 것

- r 열·r 슬라이더 — 이번 스코프 밖이다. 만들지 않는다.
- 두 번째 화살표(닻을 고르는 장치) — 닻은 언제나 내 빌드(전제 치환 포함) 하나다.
- 막대에 맞바꾸기 잔존 — 파괴 동작은 드로어·대화상자의 단추만.
- 대난투 DPS 변형 지수, 대난투 축 곡선.
- 자버프 편집의 설정 페이지 이동 — 바로가기만.
- 폴드 상태 외의 새 localStorage 키 남발.
- 이 문서 밖의 리팩터링.
